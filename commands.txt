uvicorn app.main:app --reload

http://127.0.0.1:8000/
http://127.0.0.1:8000/docs

rm -rf alembic/versions/*
alembic revision --autogenerate -m "init"
alembic upgrade head


добавить тесты с негативными сценариями

и вообще переписать тесты
написать функцию где не пройдет тест, потом переделать функцию и сделать ей минимальный функционал,
чтоб она прошла тест и потом рефакторинг


set DATABASE_URL="postgresql+asyncpg://postgres:postgres@localhost:5432/test_bdd_db"
set SECRET_KEY="BHmmwDMkJbMscV-nja5w7WdjOurRE5DNmvco2078F3k"


behave -n "создание студента с дублирующимся email"
behave -n "получение несуществующего студента"
behave -n "создание дисциплины с дублирующимся названием"
behave -n "получение несуществующей дисциплины"
behave -n "создание дублирующейся оценки"
behave -n "получение несуществующей оценки"
